# GASH
web-shell project thing i made, but to GH

## Development

```sh
npm install
npm run dev      # dev server
npm run build    # typecheck (tsc) + production build
npm run preview  # serve ./dist
npm test         # headless end-to-end smoke test (jsdom)
```
